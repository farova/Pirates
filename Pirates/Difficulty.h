#if !defined(_DIFFICULTY_ENUM)
#define _DIFFICULTY_ENUM

namespace Constants
{
    enum Difficulty
    {
        DifficultyEasy,
        DifficultyMedium,
        DifficultyHard
    };
}

#endif